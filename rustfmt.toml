edition = "2021"
max_width = 100
use_small_heuristics = "Max"
reorder_imports = true

# Regras abaixo exigem nightly — ativar quando CI tiver toolchain nightly opcional:
# imports_granularity = "Crate"
# group_imports      = "StdExternalCrate"
